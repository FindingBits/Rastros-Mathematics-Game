#include <stdio.h>
#include <string.h>
#include "interface.h"
#include "logica.h"
#include "dados.h"
int main() {
    int x;
    entrada();
    interpretador(e);
    while(1){
        int jogo = jogar(?);
    }
    fim(e->jogador_atual);
    return 0;
}
